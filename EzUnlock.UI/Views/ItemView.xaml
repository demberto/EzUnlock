<UserControl x:Class="EzUnlock.UI.Views.ItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:EzUnlock.UI.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ItemViewModel}"
             d:DesignHeight="60" d:DesignWidth="500">
    <Grid d:Background="White" Width="Auto">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="40"/>
        </Grid.ColumnDefinitions>
        <Image Grid.Column="0" Margin="6" Source="{Binding IconSource}"/>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="3*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>
            <Label Grid.Row="0" d:Content="Name" Content="{Binding Name}" FontSize="16" VerticalAlignment="Center"/>
            <Label Grid.Row="1" d:Content="Location" Content="{Binding Location}" VerticalAlignment="Center"/>
        </Grid>
        <WrapPanel Grid.Column="2" ItemHeight="32" ItemWidth="32" Margin="4" VerticalAlignment="Center">
            <Button Background="Transparent" BorderThickness="0" FontSize="14" Content="❌"/>
        </WrapPanel>
    </Grid>
</UserControl>
